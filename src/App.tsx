import "./App.css";

function App() {
  return (
    <main className="welcome-screen">
      <section className="welcome-card" aria-labelledby="welcome-title">
        <p className="eyebrow">RMail</p>
        <h1 id="welcome-title">Your calm email workspace.</h1>
        <p>
          The mail experience is being prepared. Account setup will be the first
          workflow available in the MVP.
        </p>
      </section>
    </main>
  );
}

export default App;
